// Find the size of a vector using `size()`.
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> numbers = {5, 10, 15, 20, 25};

    // size of the vector
    cout << "the Size of the vector is: " << numbers.size() << endl;

    return 0;
}

