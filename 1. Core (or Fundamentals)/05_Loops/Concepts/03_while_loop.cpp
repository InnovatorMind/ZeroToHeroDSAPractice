#include <iostream>
using namespace std;

int main() {
    int count = 1;

    // Print numbers from 1 to 5 using a while loop
    while (count <= 5) {
        cout << "Count: " << count << endl;
        count++;
    }

    return 0;
}
