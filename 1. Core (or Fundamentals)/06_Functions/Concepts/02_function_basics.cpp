#include <iostream>
using namespace std;

// Function declaration
void greet() {
    cout << "Hello, welcome to C++ functions!" << endl;
}

int main() {
    greet();  // Function call
    return 0;
}

// Explanation:

// greet() is a simple function that prints a message.
// It is called inside main(), and the function executes when called.